# DermaScan backend dependencies
# Pinned to versions known to provide prebuilt wheels for Python 3.10 (Docker base).

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9      # required by FastAPI for multipart/form-data uploads
pydantic==2.10.0

tensorflow==2.20.0           # CNN + MobileNetV2 inference and training
numpy==2.1.0
pillow==11.0.0               # in-memory image decode/encode
matplotlib==3.10.0            # jet colormap for Grad-CAM rendering

# Training/analysis only (not needed at request time, but used by train_and_save.py)
scikit-learn==1.5.2
seaborn==0.13.2

# Tests
pytest==8.2.2
httpx==0.27.0                # FastAPI TestClient transport
